# Directus CMS environment example
# Copy to cms/.env and adjust values for your deployment.

# Base public URL of your Directus server (must be accessible from the internet)
PUBLIC_URL=https://cms.your-domain.com
# Some scripts also read DIRECTUS_URL; keep it in sync
DIRECTUS_URL=${PUBLIC_URL}

# Admin credentials used by setup scripts (temporary account recommended)
# Option A: Use email/password (script will login and get a token)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=change-me-please
# Option B: Use a pre-generated admin token (preferred, more secure)
ADMIN_TOKEN=

# CORS configuration for Directus
# Allow your Vercel domains (Production + Preview), and local dev if needed
CORS_ENABLED=true
CORS_ORIGIN=https://your-frontend.vercel.app,https://*.vercel.app,http://localhost:3000
CORS_METHODS=GET,POST,PATCH,DELETE,OPTIONS
CORS_ALLOWED_HEADERS=Content-Type,Authorization
CORS_EXPOSED_HEADERS=Content-Range
# Set to true only if you use cookies/auth from the browser
CORS_CREDENTIALS=false
CORS_MAX_AGE=18000

# Recommended cookie settings if you enable auth flows in the browser
REFRESH_TOKEN_COOKIE_SECURE=true
REFRESH_TOKEN_COOKIE_SAME_SITE=lax